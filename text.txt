        if (data.results) {
          console.log(data.results);

          // setProducts bilan yangi array yaratamiz va eski qiymatni saqlaymiz
          const newProducts = data.results.map((item) => {
            return import.meta.env.VITE_API_DUMMYJOSON + "products/" + item.category;
          });

          // Yangi arrayni statega qo'shamiz
          setProducts((prevProducts) => [...prevProducts, ...newProducts]);
          setBanner(data.results);
          console.log(products)

        } else {
          console.log(data);

          const newProducts = data.map((item) => {
            return import.meta.env.VITE_API_DUMMYJOSON + "products/" + item.category;
          });
          
          console.log(products)
          setProducts((prevProducts) => [...prevProducts, ...newProducts]);
          setBanner(data);
        }